name: Build YasuPython Gemini-AI

on:
  push:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          path: main_repo

      - name: Checkout MicroPython
        uses: actions/checkout@v4
        with:
          repository: micropython/micropython
          path: micropython
          submodules: recursive

      - name: Setup ESP-IDF
        run: |
          git clone --recursive --branch v5.2.2 https://github.com/espressif/esp-idf.git esp-idf
          cd esp-idf
          ./install.sh esp32s3
          
      - name: Build and AI Auto-Fix
        shell: bash
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cd esp-idf
          source export.sh
          cd ../micropython
          make -C mpy-cross
          cd ports/esp32
          
          # İlk derleme denemesi
          make BOARD=ESP32_GENERIC_S3 BOARD_VARIANT=SPIRAM_OCT \
               USER_C_MODULES=${{ github.workspace }}/main_repo/micropython.cmake \
               ESP_IDF_PATH=${{ github.workspace }}/esp-idf > build_log.txt 2>&1 && OK=true || OK=false

          if [ "$OK" = false ]; then
            echo "!!! HATA ANALIZI BASLIYOR !!!"
            
            # Python kodunun girintileri YAML'a göre ayarlandı
            cat << 'EOF' > ai_fix.py
import os, google.generativeai as genai, sys
api_key = os.environ.get("GEMINI_API_KEY")
if not api_key:
    sys.exit(1)
genai.configure(api_key=api_key)
model = genai.GenerativeModel('gemini-1.5-flash')
try:
    with open("build_log.txt", "r") as f:
        log = f.read()[-3000:]
    system_context = "ESP32-S3 MicroPython 'esp_wifi_80211_tx.c' modülü hatası düzeltici."
    prompt = f"{system_context}\n\nLog:\n{log}\n\nSADECE bash komutları yaz."
    response = model.generate_content(prompt)
    content = response.text
    code = content.split("```bash")[1].split("```")[0] if "```bash" in content else content
    with open("fix.sh", "w") as f:
        f.write(code.strip())
except Exception as e:
    print(e)
EOF

            python3 -m pip install -q -U google-generativeai
            python3 ai_fix.py
            
            if [ -f fix.sh ]; then
              chmod +x fix.sh
              ./fix.sh
              make BOARD=ESP32_GENERIC_S3 BOARD_VARIANT=SPIRAM_OCT \
                   USER_C_MODULES=${{ github.workspace }}/main_repo/micropython.cmake \
                   ESP_IDF_PATH=${{ github.workspace }}/esp-idf
            fi
          fi

      - name: Upload Firmware
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: YasuPython-S3-Firmware
          path: micropython/ports/esp32/build-ESP32_GENERIC_S3-SPIRAM_OCT/firmware.bin
